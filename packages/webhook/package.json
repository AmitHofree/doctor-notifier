{
  "name": "webhook",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "publish": "wrangler publish",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}